<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="css/styles.css">
</head>

<body>
  <nav>
    <a href="/">Home</a>

    <% if (user) { %>
    <a href="/playlists?user=<%=user._id%>">My Playlists</a> <% } %>

    <a href="/playlists">Playlists</a> <a href="/songs">Songs</a>
  </nav>
  <div class="login"><% if (user) { %>
    <a href="/logout">Log Out</a>
    <% } else { %>
    <a href="/auth/google"></i>Login with Google</a>
    <% } %></div>
  </div>
  <h1><%= title %></h1>
  <!-- prettier-ignore-attribute -->