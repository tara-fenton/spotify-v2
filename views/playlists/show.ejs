<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
   
    <div>
        <div><%= playlist.title %></div>
        <% playlist.songs.forEach(function(p) {%>
            <div>
                song object:<%= songs.filter(song => song._id == p)%>
               

            </div>
            <% }); %>  
    </body>
    
    </html>