<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
   
    <div>
        <div class="container">
            <% playlists.forEach(function(p) {%>
            <div>
                <a href="playlists/<%=p._id%>"><%=p.title%></a>

            </div>
            <% p.songs.forEach(function(s) {%>
            <div>
              <%=s%>

          </div>
          <% }); %>

            </a>
            <td><a href="/playlists/<%= p._id %>/edit">Edit</a>
            </td>
            <form action='/playlists/<%= p._id %>?_method=DELETE' method="POST"><button>X</button></form>
    
        </div>
        <% }); %>
    </div>
    <a href="/playlists/new">Add Playlist</a>
    </body>
    
    </html>