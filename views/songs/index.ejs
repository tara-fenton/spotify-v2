<%- include('../partials/header') %>
<div>
  <div class="container">
    <% songs.forEach(function(s) {%>
    <div>Title: <%=s.title%></div>
    <div>Artist: <%=s.artist%></div>
    <td><a href="/songs/<%= s._id %>/edit">Edit</a></td>
    <td>
      <form action="/songs/<%= s._id %>?_method=DELETE" method="POST">
        <button>X</button>
      </form>
    </td>
  </div>
  <hr />
  <% }); %>
</div>
<br />
<a href="/songs/new">ADD SONG</a>
</body>

</html>